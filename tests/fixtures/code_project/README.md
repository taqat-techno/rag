# Demo Project

A small demo app used to exercise code-aware indexing.

## Architecture

The app has an authentication service and an API router. The auth service
issues tokens; the API router maps paths to handlers.
